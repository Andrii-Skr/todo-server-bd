export type NoteAction = {
  getNotes: string;
  getStats: string;
  update: string;
  loading: string;
};

export const noteAction = {
  getNotes: "getNotes",
  getStats: "getStats",
  update: "update",
  loading: "loading",
};
